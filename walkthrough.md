# סיכום ביצוע ומפת דרכים: ארקייד הבטטה 🍠

הפרויקט הוא פלטפורמת משחקים נוסטלגית המבוססת על דפי אינטרנט סטטיים וספריות תצוגה מודרניות. 

---

## תשתית נוכחית ומצב הפרויקט

### 1. קבצים וניהול קוד
- **index.html**: קובץ התצוגה הראשי של הלובי, הכולל את תצוגות המשחק, חלונות המידע, לוח התוצאות ומסכי הכניסה/רישום.
- **src/styles/style.css**: קובץ עיצוב ייחודי עבור אנימציות, אפקטים נוסטלגיים ועיצוב כללי של הפלטפורמה.
- **src/core/**: תיקיית הליבה שמנהלת את מצב המשחק, רנדור על גבי Canvas, ממשק מגע ומקלדת, ומעברי מסכים.
- **src/games/**: 40 משחקים שונים שירשו מבנה מחלקה אחיד מתוך קובץ הבסיס `MiniGame.js`.
- **src/services/Auth.js**: מנהל רישום והתחברות משתמשים בצורה מקומית.

### 2. לוח תוצאות וחיבור לענן
לוח התוצאות שודרג מניהול מקומי בלבד לניהול משותף וגלובלי בענן:
- **Netlify Functions**: קוד השרת יושב בתיקייה `netlify/functions/leaderboard.js` ומנהל בקשות שמירה וקריאה של תוצאות.
- **Netlify Blobs**: האחסון הפנימי והמובטח של נטליפיי משמש כמסד הנתונים הראשי שמאחסן את 3 השיאים הטובים ביותר לכל שילוב של משחק ורמת קושי.
- **גיבוי מקומי**: שירות הלוח (`src/services/Leaderboard.js`) מכיל מנגנון גיבוי אוטומטי השומר את התוצאות מקומית במידה והמשתמש לא מחובר לאינטרנט או נמצא בסביבת פיתוח מקומית.

### 3. אוטומציה וחיבור לגיטהאב (GitHub)
הפרויקט מוגדר לעבודה אוטומטית מלאה מול גיטהאב:
- **כתובת המאגר**: `https://github.com/noam2015/potato-arcade.git`
- **פרסום אוטומטי (CI/CD)**: האתר מחובר ישירות לחשבון הנטליפיי שלך. בכל פעם שסוכן מבצע שינוי קוד ודוחף אותו לגיטהאב (`git push`), נטליפיי ימשוך ויעדכן את האתר החי באופן אוטומטי תוך מספר שניות.
- **מפתח אימות לגיט**: מוגדר מפתח אימות (Personal Access Token) לשימוש בגיט מקומי כך שניתן לדחוף שינויים ללא צורך באימות ידני.

---

## הנחיות לעבודה עבור סוכנים (AI) בעתיד
כשאתה מתחיל סשן חדש ועובד על הפרויקט הזה:
1. **שינויי קוד**: בצע את השינויים בקבצים המתאימים בתיקיות `src/` או `netlify/`.
2. **בדיקות עברית**: הקפד תמיד לשמור את הקבצים בקידוד **UTF-8 ללא BOM** כדי למנוע שיבושים בתווי עברית בדפדפן.
3. **שמירה ועדכון**: בסיום כל שלב עבודה, יש להריץ את הפקודות הבאות כדי לעדכן את האתר באוויר:
   ```bash
   git add .
   git commit -m "תיאור השינוי שבוצע"
   git push origin master
   ```
4. **אחסון ובלובס**: אין צורך להגדיר מפתחות סודיים או מסדי נתונים חיצוניים לפרויקט. כל הנתונים של לוח התוצאות מנוהלים אוטומטית דרך מערכת הבלובס המובנית של נטליפיי.
