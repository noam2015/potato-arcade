# מדריך העלאת ארקייד הבטטה לאינטרנט (ענן) 🌐 🍠

אם ברצונך שהמשחק יהיה נגיש לכולם **מכל מקום בעולם, מכל מחשב או טלפון, ובכל זמן (24/7)**, הדרך הטובה ביותר היא להעלות אותו לאתר אינטרנט אמיתי בחינם. 

להלן שתי הדרכים הקלות ביותר לעשות זאת:

---

## 🚀 אפשרות א': העלאה מהירה ללא התקנה באמצעות Netlify (מומלץ ופשוט ביותר!)

זוהי הדרך המהירה ביותר. היא לוקחת פחות מ-30 שניות ולא דורשת שום כלי במחשב:

1. פתח את הדפדפן והיכנס לאתר: [app.netlify.com/drop](https://app.netlify.com/drop) (ניתן ליצור משתמש חינמי בשניות).
2. פתח את תיקיית הפרויקט במחשב שלך (`ארקייד הבטטה`).
3. **גרור ושחרר (Drag & Drop)** את כל התיקייה ישירות אל תוך המלבן המסומן באתר של Netlify.
4. תוך 5 שניות האתר יסיים להעלות את הקבצים ויציג לך קישור ציבורי ייחודי (למשל: `https://shiny-potato-arcade.netlify.app`).
5. **זהו!** שלח את הקישור לחברים והם יוכלו לשחק מכל מחשב או טלפון בעולם.

> [!TIP]
> בהגדרות האתר בתוך Netlify תוכל לשנות את השם של הקישור (ה-Domain) לכל שם פנוי שתרצה (למשל `potato-arcade.netlify.app`).

---

## 🐙 אפשרות ב': העלאה דרך GitHub Pages (מושלם למפתחים)

מאחר שכבר מוגדר Git בתיקייה שלך, תוכל להעלות את הקוד ל-GitHub ולהפעיל את אתר המשחקים ישירות משם:

1. צור מאגר (Repository) חדש ב-[GitHub](https://github.com) וקרא לו למשל `potato-arcade`.
2. קשר את הפרויקט המקומי שלך למאגר החדש והעלה את הקוד על ידי הרצת הפקודות הבאות בטרמינל בתוך תיקיית המשחק:
   ```bash
   git remote add origin https://github.com/המשתמש-שלך/potato-arcade.git
   git branch -M main
   git add .
   git commit -m "העלאה ראשונית של הארקייד"
   git push -u origin main
   ```
3. לאחר שהקוד עלה ל-GitHub, היכנס להגדרות המאגר בארבעה שלבים פשוטים:
   - לחץ על לשונית **Settings** (הגדרות) ב-GitHub.
   - בתפריט הצדדי, בחר ב-**Pages**.
   - תחת **Build and deployment**, בחר בבסיס המקור כ-`Deploy from a branch`.
   - תחת **Branch**, בחר ב-`main` (ובתיקייה `/root`) ולחץ על **Save** (שמור).
4. תוך כדקה, GitHub ייצר עבורך קישור מאובטח וקבוע למשחק בכתובת: `https://username.github.io/potato-arcade/`.
